package cs3500.animator.controller;

/**
 * Represents an animation controller. This allows for the animation to run.
 */
public interface AnimationController {

  /**
   * The run method handles running the animation based on the model and view.
   */
  void run();
}
